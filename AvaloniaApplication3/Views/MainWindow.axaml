<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" d:DesignWidth="1200" d:DesignHeight="700"
		Width="1200" Height="700"
        MinWidth="1200" MinHeight="700"
        x:Class="AvaloniaApplication3.MainWindow"
		xmlns:vm="clr-namespace:AvaloniaApplication3.ViewModels"
        xmlns:view="clr-namespace:AvaloniaApplication3.Views"
		x:DataType="vm:MainViewModel"
        Title="AvaloniaApplication3">
<Window.DataContext>
	<vm:MainViewModel></vm:MainViewModel>
</Window.DataContext>
	
	<Grid Background="{DynamicResource PrimaryBackground}"  ColumnDefinitions="Auto,*">
		
<ContentControl Grid.Column="1" Content="{Binding  CurrentPage}"></ContentControl>		
		
		<Border Grid.Column="0"   Padding="5" Background="{DynamicResource PrimaryBackgroundGradient}">
			<Grid RowDefinitions="*,Auto">
				<StackPanel Grid.Row="0" Spacing="10">
					<Image PointerPressed="InputElement_OnPointerPressed" Width="150"     Margin="0,-20,0,0" Source="{Binding SideMenuImage}" IsVisible="{Binding Path=SideMenuExpanded}"></Image>
					<Image PointerPressed="InputElement_OnPointerPressed" Width="50"     Margin="0,-20,0,0" Source="{Binding SideMenuImage}" IsVisible="{Binding Path=!SideMenuExpanded}"></Image>
					<iconButton Classes="subtle" IconText="" HorizontalAlignment="Stretch" VerticalAlignment="Bottom" Classes.active="{Binding HomePageIsActive}" Command="{Binding GotoHomeCommand}">
						<Label  Content="Home11" IsVisible="{Binding Path=SideMenuExpanded}"></Label>
					</iconButton>
					<iconButton Classes="subtle" IconText="" HorizontalAlignment="Stretch" Classes.active="{Binding ProcessPageIsActive}"  Command="{Binding GotoProcessCommand}">
						<Label Content="Process" IsVisible="{Binding Path=SideMenuExpanded}"></Label>
					</iconButton>
					<iconButton Classes="subtle" IconText="" HorizontalAlignment="Stretch"  Classes.active="{Binding ActionPageIsActive}" Command="{Binding GotoActionCommand}">
						<Label Content="Action" IsVisible="{Binding Path=SideMenuExpanded}"></Label>
					</iconButton>
					<iconButton Classes="subtle" IconText=""  HorizontalAlignment="Stretch" Classes.active="{Binding MacrosPageIsActive}" Command="{Binding GotoMacrosCommand}">
						<Label Content="Macros" IsVisible="{Binding Path=SideMenuExpanded}"></Label>
					</iconButton>
					<iconButton Classes="subtle" IconText="" HorizontalAlignment="Stretch" Classes.active="{Binding ReporterPageIsActive}" Command="{Binding GotoReporterCommand}">
						<Label Content="Reporter" IsVisible="{Binding Path=SideMenuExpanded}"></Label>
					</iconButton>
					<iconButton Classes="subtle" IconText="" HorizontalAlignment="Stretch" Classes.active="{Binding HistoryPageIsActive}" Command="{Binding GotoHistoryCommand}">
						<Label  Content="History" IsVisible="{Binding Path=SideMenuExpanded}"></Label>
					</iconButton>
					
				</StackPanel>
				<Button Grid.Row="1" Classes="subtle" HorizontalAlignment="Left" Classes.active="{Binding SettingPageIsActive}"  Command="{Binding GotoSettingCommand}"	>
					<Border Classes="icon-only"
                           >
						<Label Classes="icon-only" Content="" />
					</Border>
				</Button>
			</Grid>
		</Border>
	</Grid>
	
 
</Window>
